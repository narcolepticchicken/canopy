{
  "name": "canopy",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "pkg/*"],
  "license": "MIT",
  "packageManager": "pnpm@10.0.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm --filter mcp-server dev"
  }
}
